class Project {
  constructor(
    id,
    title,
    description,
    imageUrl,
    duration

  
  ){
    this.id=id;
    this.title=title;
    this.description=description;
    this.imageUrl=imageUrl;
    this.duration=duration;
  }
}

export default Project;